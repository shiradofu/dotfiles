# .env
# DB_HOST=127.0.0.1
# DB_PORT=$(rand port)

services:
  app:
    environment:
      DB_HOST: db
      DB_PORT: 3306
  db:
    # if using Apple Silicon
    # platform: linux/amd64
    ports:
      - ${DB_PORT}:3306
